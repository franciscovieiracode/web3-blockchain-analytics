import { ContactsPipePipe } from './contacts-pipe.pipe';

describe('ContactsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ContactsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
