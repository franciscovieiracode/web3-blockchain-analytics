import { TransactionlistPipe } from './transactionlist.pipe';

describe('TransactionlistPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionlistPipe();
    expect(pipe).toBeTruthy();
  });
});
